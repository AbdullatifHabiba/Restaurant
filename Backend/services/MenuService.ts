export class menuserice {
    static serve(req) {
        switch (req.service) {
            case "getAll":

                break;
            case "get5":

                break;
            default:
                return "error";
                break;
        }
    }
}